# AI Growth Notes v1.4.0 Cloud Edition

SQLite実行系をPostgreSQLへ統一し、旧3表を移行できるread-only migrationを追加しました。Render HTTPS公開、health check、単一ユーザーArgon2認証、署名付きCookie session、CSRF、open redirect防止、軽量login制限を含みます。

Upgrade前にSQLiteをbackupし、`--dry-run`、本Migration、件数照合、主要機能確認の順に実施してください。本番設定が欠ける場合は安全に起動を拒否します。
